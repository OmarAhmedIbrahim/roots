import 'dart:ui';

class CustomButtonClass {
  String buttonText;
  Color? textColor;
  Color? buttonColor;
  CustomButtonClass(
      {required this.buttonText, this.textColor, this.buttonColor});
}
